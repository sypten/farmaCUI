---
import Layout from '../../layouts/Layout.astro';
import AdminGuard from '../../components/AdminGuard.jsx';
import BannerManager from '../../components/admin/BannerManager.jsx';
---

<Layout title="Gestión de Banners - Admin">
  <AdminGuard client:only="react">
    <div class="bg-gray-50 min-h-screen">
      <header class="bg-white shadow mb-8 sticky top-0 z-40">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
          <div class="flex items-center gap-2">
            <span class="text-2xl">🎨</span>
            <h1 class="text-xl font-bold text-farma-primary">Panel Administrativo</h1>
          </div>
          <nav class="flex gap-6 text-sm font-medium">
            <a href="/admin" class="text-gray-500 hover:text-farma-primary transition">📦 Productos</a>
            <a href="/admin/ventas" class="text-gray-500 hover:text-farma-primary transition">💰 Ventas</a>
            <a href="/admin/banners" class="text-farma-primary font-bold border-b-2 border-farma-primary pb-1">🖼️ Banners</a>
            <a href="/" class="text-gray-400 hover:text-farma-secondary ml-4 border-l border-gray-300 pl-4">Ir a la tienda ↗</a>
          </nav>
        </div>
      </header>

      <main class="container mx-auto px-4 pb-12">
        <BannerManager client:only="react" />
      </main>
    </div>
  </AdminGuard>
</Layout>